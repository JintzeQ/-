name: Edge Search Round 10
on:
  push:
    branches: [edge-search-r10]
  workflow_dispatch:
jobs:
  search:
    runs-on: ubuntu-latest
    timeout-minutes: 340
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pandas numpy requests
      - run: python edge_search_round10.py | tee edge_search_r10.log
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: edge-search-round10-results
          path: |
            r10_output/
            edge_search_r10.log
          retention-days: 7
