name: Edge Search Round 5
on:
  push:
    branches: [edge-search-r5]
  workflow_dispatch:
jobs:
  search:
    runs-on: ubuntu-latest
    timeout-minutes: 340
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pandas numpy requests
      - name: Run volume profile search
        run: python edge_search_round5.py | tee edge_search_r5.log
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: edge-search-round5-results
          path: |
            r5_output/
            edge_search_r5.log
          retention-days: 7
